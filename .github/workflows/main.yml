name: Test
on:
  push:
    branches:
      - main
    paths:
      - 'action.yml'
jobs:
  test-github-action:
    runs-on: ubuntu-24.04
    name: Test GitHub Action
    steps:
      - uses: actions/checkout@v5
      - uses: ./